SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=
PORT=3000

# Required for MVP project creation.
# Must be an existing Supabase users.id UUID, not a random UUID.
FACADEFLOW_MVP_OWNER_ID=<existing users.id UUID>
